package xyz.bookself.users.controllers;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import static org.assertj.core.api.AssertionsForClassTypes.assertThat;

@SpringBootTest
public class UserControllerTest {
    /*
    @Test
    void givenTwoBooks_whenTheirIdsAreTheSame_thenTheyAreConsideredToBeEqual() {
        final String id = "001";
        final Book a = new Book();
        a.setId(id);
        final Book b = new Book();
        b.setId(id);
        assertThat(a.equals(b)).isTrue();
    }

    @Test
    void givenTwoAuthors_whenTheirIdsAreTheSame_thenTheyAreConsideredToBeEqual() {
        final String id = "001";
        final Author a = new Author();
        a.setId(id);
        final Author b = new Author();
        b.setId(id);
        assertThat(a.equals(b)).isTrue();
    }

    @Test
    void givenABookObject_whenTheObjectIsNotNull_thenHashCodeReturnsAnInteger() {
        assertThat(new Book().hashCode()).isBetween(Integer.MIN_VALUE, Integer.MAX_VALUE);
    }

    @Test
    void givenAnAuthorObject_whenTheObjectIsNotNull_thenHashCodeReturnsAnInteger() {
        assertThat(new Author().hashCode()).isBetween(Integer.MIN_VALUE, Integer.MAX_VALUE);
    }
    */
}